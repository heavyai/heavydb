#define PRINT_SLINE(n) for (int i = 0; i < n; ++i) printf("-"); printf("\n");
#define PRINT_DLINE(n) for (int i = 0; i < n; ++i) printf("="); printf("\n");
#define PRINT_DEBUG(msg) printf("[%s:%d] %s\n", __func__, __LINE__, msg);
