set(bench_name taxi_reduced)
find_package(fmt REQUIRED)
set(EXECUTE_TEST_LIBS gtest ArrowQueryRunner fmt::fmt ${MAPD_LIBRARIES} ${Arrow_LIBRARIES} ${CMAKE_DL_LIBS} ${CUDA_LIBRARIES} ${Boost_LIBRARIES} ${ZLIB_LIBRARIES})
set(THRIFT_HANDLER_TEST_LIBRARIES thrift_handler ${EXECUTE_TEST_LIBS})
add_executable(${bench_name} taxi_reduced_bench.cpp)
target_link_libraries(${bench_name} ${EXECUTE_TEST_LIBS} benchmark)